---
label: About
hide_body: true
fields:
- name: headshot
  type: file
  config:
    maxSize: 250
  label: Headshot
- name: biography
  type: textarea
  default: ''
  config:
    required: true
    wysiwyg: true
    schema:
      format: markdown
  label: Biography
- name: email
  type: text
  config:
    required: true
  label: Email
- name: links
  type: include
  config: {}
  template: link
  label: Links
  description: any other link (Facebook, Spotlight etc...)
- name: cv
  type: file
  config:
    maxSize: 250
  label: CV
pages:
- content/pages/about.md
